/**
 * Default preset when none is stored. Full list is built at dev/build time into
 * `public/banners/manifest.json` (see `scripts/generateBannerManifest.mjs`).
 */
export const DEFAULT_PROFILE_BANNER_PRESET_PATH = "banners/default.svg";
