// tuf-search: #ProfileEditRedirect #profileEditRedirect #account #settings
import { Navigate } from "react-router-dom";

const ProfileEditRedirect = () => <Navigate to="/settings/account" replace />;

export default ProfileEditRedirect;
